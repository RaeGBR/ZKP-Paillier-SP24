{
  "name": "cryptoplus",
  "version": "4.0.0",
  "description": "Crypto Plus Library",
  "main": "Makefile",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha ./test/js/*.test.js --reporter mocha-multi-reporters --reporter-options configFile=mocha-config.json"
  },
  "keywords": [
    "Cryptography",
    "WebAssembly"
  ],
  "gypfile": false,
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "mocha-junit-reporter": "^1.18.0",
    "mocha-multi-reporters": "^1.1.7"
  }
}
